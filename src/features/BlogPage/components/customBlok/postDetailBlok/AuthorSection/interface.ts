export interface AuthorSectionProps {
    blok: {
      authorImage?: {
        filename: string;
      };
      authorName?: string;
      blogDate?: string;
      readingTime?: string;
      blogTitle?: string;
    };
  }